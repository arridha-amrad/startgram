services:
  db:
    image: postgres:18-alpine
    container_name: startgram-container
    restart: always
    environment:
      POSTGRES_USER: ari
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: startgram
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
